version: '3'
services:

  nest:
    container_name: nest_app
    build:
      context: ./
    restart: always
    ports:
      - 3030:3000
    environment:
      URI_MONGODB=mongodb://localhost:27018/ticketsbd
      PORT=3000
      JWT_SECRET=AEIUIJALKJLAKJLKSX!
    depends_on:
      - db



  db: 
    image: mongo:5
    restart: always
    ports:
      - 27018:27017
    environment:
      MONGO_DATABASE: ticketsbd
    volumes:
      - ./mongo:/data/db
  